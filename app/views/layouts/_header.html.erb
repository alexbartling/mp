<div class="navbar">
        <div class="navbar-inner">
          <div class="container-fluid">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </a>
            <a class="brand" href="#" name="top">Brand Name</a>
            <div class="nav-collapse collapse">
              <ul class="nav">
                <li><a href="#"><i class="icon-home"></i> Home</a></li>
                <li class="divider-vertical"></li>
               <li class: "active"> <%= link_to 'Sign Out', destroy_user_session_path, :method => :delete %></li>
                <!-- <li class="active"><a href="#"><i class="icon-file"></i> Pages</a></li> -->
                <li class="divider-vertical"></li>
                <li><a href="#"><i class="icon-envelope"></i> Messages</a></li>
                <li class="divider-vertical"></li>
                          <li><a href="#"><i class="icon-signal"></i>Hero Stats</a></li> 
                <% if user_signed_in? %>
                <li class="divider-vertical"></li>
                <li><%= link_to '<i class="icon-unlock"></i>Profile'.html_safe, current_user %></li>
         <!--        <li><a href="#"><i class="icon-unlock"></i> Profile</a></li> -->
              </ul>
                <% else %>
                <ul class="nav pull-right">
                <li><a href="/signup">Sign Up</a></li>
                          <li class="divider-vertical"></li>
                <li class="dropdown">
                  <a class="dropdown-toggle" href="#" data-toggle="dropdown">Sign In <strong class="caret"></strong></a>
                  <div class="dropdown-menu" style="padding: 15px; padding-bottom: 1px;">
                    
                      <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
                      <%= f.email_field :email, :autofocus => true, style: "margin-bottom: 15px;", placeholder:"Email"%>
                      <!-- <input style="margin-bottom: 15px;" type="password" placeholder:"Password" id="password" name="password"> -->
                        <%= f.password_field :password, placeholder: "Password", style: "margin-bottom: 15px;" %>
                      <!-- <input style="float: left; margin-right: 10px;" type="checkbox" name="remember-me" id="remember-me" value="1"> -->
                        <% if devise_mapping.rememberable? -%>
                        <%= f.check_box :remember_me, style: "float: left; margin-right: 10px;" %> 
                        <%= f.label :remember_me, class: "string optional" %>
                        <% end %>
                        <%= f.submit "Sign in", class: "btn btn-primary btn-block", id: "sign-in"%>
                        <%= render "devise/shared/links" %>
                      <% end %>
                     
              <% end %>
                      <!-- <input class="btn btn-primary btn-block" type="submit" id="sign-in" value="Sign In"> -->
                      <!-- <label style="text-align:center;margin-top:5px">or</label>
                                      <input class="btn btn-primary btn-block" type="button" id="sign-in-google" value="Sign In with Google">
                      <input class="btn btn-primary btn-block" type="button" id="sign-in-twitter" value="Sign In with Twitter"> -->
                    
                 





                  </div>
                </li>
              </ul>
            </div>
            <!--/.nav-collapse -->
          </div>
          <!--/.container-fluid -->
        </div>
        <!--/.navbar-inner -->
      </div>
      <!--/.navbar -->